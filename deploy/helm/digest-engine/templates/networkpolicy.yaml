{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "digest-engine.fullname" . }}-default-deny
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "digest-engine.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "digest-engine.fullname" . }}-allow-nginx
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "nginx") | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - port: 80
          protocol: TCP
  egress:
    - to:
        - podSelector:
            matchLabels:
              {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "django") | nindent 14 }}
      ports:
        - port: {{ .Values.django.port }}
          protocol: TCP
    - to:
        - namespaceSelector: {}
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "digest-engine.fullname" . }}-allow-django
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "django") | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "nginx") | nindent 14 }}
      ports:
        - port: {{ .Values.django.port }}
          protocol: TCP
  egress:
    - to:
        - podSelector:
            matchLabels:
              {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "postgres") | nindent 14 }}
      ports:
        - port: {{ .Values.postgres.service.port }}
          protocol: TCP
    - to:
        - podSelector:
            matchLabels:
              {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "redis") | nindent 14 }}
      ports:
        - port: {{ .Values.redis.service.port }}
          protocol: TCP
    - to:
        - podSelector:
            matchLabels:
              {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "qdrant") | nindent 14 }}
      ports:
        - port: {{ .Values.qdrant.service.port }}
          protocol: TCP
    - to:
        - namespaceSelector: {}
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "digest-engine.fullname" . }}-allow-worker-and-beat
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  podSelector:
    matchExpressions:
      - key: app.kubernetes.io/component
        operator: In
        values:
          - celery-worker
          - celery-beat
      - key: app.kubernetes.io/name
        operator: In
        values:
          - {{ include "digest-engine.name" . }}
      - key: app.kubernetes.io/instance
        operator: In
        values:
          - {{ .Release.Name }}
  policyTypes:
    - Egress
  egress:
    - to:
        - podSelector:
            matchLabels:
              {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "postgres") | nindent 14 }}
      ports:
        - port: {{ .Values.postgres.service.port }}
          protocol: TCP
    - to:
        - podSelector:
            matchLabels:
              {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "redis") | nindent 14 }}
      ports:
        - port: {{ .Values.redis.service.port }}
          protocol: TCP
    - to:
        - podSelector:
            matchLabels:
              {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "qdrant") | nindent 14 }}
      ports:
        - port: {{ .Values.qdrant.service.port }}
          protocol: TCP
    - to:
        - namespaceSelector: {}
      ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "digest-engine.fullname" . }}-allow-postgres
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "postgres") | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchExpressions:
              - key: app.kubernetes.io/component
                operator: In
                values:
                  - django
                  - celery-worker
                  - celery-beat
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - {{ include "digest-engine.name" . }}
              - key: app.kubernetes.io/instance
                operator: In
                values:
                  - {{ .Release.Name }}
      ports:
        - port: {{ .Values.postgres.service.port }}
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "digest-engine.fullname" . }}-allow-redis
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "redis") | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchExpressions:
              - key: app.kubernetes.io/component
                operator: In
                values:
                  - django
                  - celery-worker
                  - celery-beat
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - {{ include "digest-engine.name" . }}
              - key: app.kubernetes.io/instance
                operator: In
                values:
                  - {{ .Release.Name }}
      ports:
        - port: {{ .Values.redis.service.port }}
          protocol: TCP
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "digest-engine.fullname" . }}-allow-qdrant
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "qdrant") | nindent 6 }}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchExpressions:
              - key: app.kubernetes.io/component
                operator: In
                values:
                  - django
                  - celery-worker
                  - celery-beat
              - key: app.kubernetes.io/name
                operator: In
                values:
                  - {{ include "digest-engine.name" . }}
              - key: app.kubernetes.io/instance
                operator: In
                values:
                  - {{ .Release.Name }}
      ports:
        - port: {{ .Values.qdrant.service.port }}
          protocol: TCP
{{- end }}
