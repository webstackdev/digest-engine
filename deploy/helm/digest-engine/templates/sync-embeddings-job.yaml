{{- if .Values.jobs.syncEmbeddings.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "digest-engine.fullname" . }}-sync-embeddings
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  template:
    metadata:
      labels:
        {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "sync-embeddings") | nindent 8 }}
    spec:
      restartPolicy: OnFailure
      containers:
        - name: sync-embeddings
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command:
            - python
            - manage.py
            - sync_embeddings
          env:
            - name: BOOTSTRAP_APP
              value: "false"
          envFrom:
            - configMapRef:
                name: {{ include "digest-engine.fullname" . }}-env
            - secretRef:
              name: {{ include "digest-engine.secretName" . }}
{{- end }}
