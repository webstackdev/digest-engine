apiVersion: v1
kind: Service
metadata:
  name: {{ include "digest-engine.fullname" . }}-nginx
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.nginx.service.type }}
  ports:
    - name: http
      port: {{ .Values.nginx.service.port }}
      targetPort: http
  selector:
    {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "nginx") | nindent 4 }}
