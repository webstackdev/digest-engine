{{- if .Values.qdrant.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "digest-engine.qdrantHost" . }}
  labels:
    {{- include "digest-engine.labels" . | nindent 4 }}
spec:
  ports:
    - name: http
      port: {{ .Values.qdrant.service.port }}
      targetPort: http
  selector:
    {{- include "digest-engine.componentLabels" (dict "Release" .Release "Values" .Values "Chart" .Chart "component" "qdrant") | nindent 4 }}
{{- end }}
